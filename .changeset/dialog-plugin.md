---
'@rrweb/rrweb-plugin-dialog-record': minor
'@rrweb/rrweb-plugin-dialog-replay': minor
---

Add dialog plugin pair that records native `window.alert` / `window.confirm` / `window.prompt` dialogs (message, prompt default value, and user response) and surfaces them during replay via an `onDialog` callback.
