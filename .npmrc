# Hoist all @types/* packages to the workspace root so TypeScript can find
# them via tsconfig.base.json's shared `types` array. Yarn 1's flat
# node_modules layout did this implicitly; pnpm's strict per-package layout
# does not, so the hoist has to be explicit.
public-hoist-pattern[]=@types/*
